How to compile

1. go inside the folder

2. load modules using setupenv.sh file:
source setupenv.sh

3. in the makefile modify ARMADILLO_INC to ge the right path for armadillo (in this folder)

4. compile using make file
make clean
make

5. run solver using script
sbatch StableSet.batch


REMARK: 5. run solver manually: for instance CubicVT26_5.dat as (last 3 are ID and output times after sometime has passed):
srun -N1 -n24 --mem=0 StableSetBaB data/CubicVT26_5.dat ID 300 3600

NOTE: Change -N and -n parameters for number of nodes and cores, respectively. 

